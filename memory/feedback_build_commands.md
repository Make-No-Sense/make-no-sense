---
name: User runs build commands themselves
description: User prefers to run npm/build commands themselves rather than having Claude execute them
type: feedback
---

User prefers to run build commands (npm run build, npm run dev, etc.) themselves rather than having Claude execute them via Bash.

**Why:** Personal preference — they want control over when builds run.

**How to apply:** When a task requires running a build or dev server check, provide the command for the user to run rather than executing it directly.
